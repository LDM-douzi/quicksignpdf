import React from "react";
import ReactDOM from "react-dom/client";
import ProEditorApp from "./ProEditorApp";
import "./proEditor.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <ProEditorApp />
  </React.StrictMode>,
);
